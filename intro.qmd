---
title: WebR in Quarto HTML Documents
format: html
engine: knitr
---

# Introduction

I have removed the following lines from the header

%filters:
%  - webr

This is a webr-enabled code cell in a Quarto HTML document.

I changed `{webr-r}` for `{r}` while a problem is fixed.

```{r}
fit = lm(mpg ~ am, data = mtcars)
summary(fit)
```

